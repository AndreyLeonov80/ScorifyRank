# x-files-client-backfront frontend

Browser UI for X-Files.

Current imported shape keeps existing HTML screens and `js/script.api.js` first. Shared CSS/API/runtime modules are migrated incrementally, and `npm run build` now emits a production static bundle into `dist`.
